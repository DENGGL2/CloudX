<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The local MuMu test gateway is HTTP; release signaling must use HTTPS. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
